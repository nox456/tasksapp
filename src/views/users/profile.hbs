<div class="h-full flex flex-col divide-y-2 divide-[#c5bde5]">
    <div class="flex justify-between py-4">
        <div class="flex items-center gap-4">
            {{> user/avatar change=true userImg=user.user_img class='w-24 h-24'}}
            <h1 class="text-2xl font-bold">{{user.username}}</h1>
        </div>
        <div class="p-4 flex items-center">
            <p><span class="text-lg font-semibold">Puntos: </span>{{points}}</p>
        </div>
    </div>
    <div class="flex flex-col items-center h-full justify-center [&>button]:w-3/5 last:[&>button]:mt-auto gap-4 py-8 sm:[&>button]:w-4/5">
        {{> buttons/button_link url='/profile/change-username' content='Cambiar Nombre de Usuario' class='py-2'}}
        {{> buttons/button_link url='/profile/change-password' content='Cambiar Contraseña' class='py-2'}}
        {{> buttons/button_link url='/profile/delete-account' content='Eliminar Cuenta' class='py-2'}}
    </div>
</div>
